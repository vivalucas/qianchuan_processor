[project]
name = "qianchuan-processor"
version = "1.5.1"
description = "Batch convert vertical ad videos to Qianchuan-friendly specs."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "pillow>=11.0.0",
    "pyinstaller>=6.0.0",
]
